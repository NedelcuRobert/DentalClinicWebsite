@if (User.Identity.IsAuthenticated)
{
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("", "Account")">Contul meu</a>
    </li>
    <li class="nav-item">
        <form class="form-inline" asp-controller="User" asp-action="Logout">
            <button class="btn btn-outline-danger my-2 my-sm-0" type="submit">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("", "Register")">Înregistrare</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="@Url.Action("", "Login")">Login</a>
    </li>
}